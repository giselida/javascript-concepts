/* 
[getElementById()]

Retorna uma referência de objeto ao elemento identificado.

O método getElementById() da interface Document retorna um objeto Element que representa o elemento cuja propriedade id corresponde à string especificada. Como os IDs dos elementos precisam ser exclusivos se especificados, eles são uma maneira útil de obter acesso rápido a um elemento específico.

*/

const $myStart = document.getElementById("start");
